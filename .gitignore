# 의존성
/node_modules
/.pnp
.pnp.*

# Next.js 빌드 산출물
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# 테스트 및 로그
/coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# 환경 변수
.env
.env*.local
.dev.vars

# 배포
.vercel

# 편집기·OS
.DS_Store
Thumbs.db
*.swp

# 실수로 만들어진 중첩 클론 (원격 저장소와 동일한 이름의 빈 디렉터리)
/kkaenam.GG/

# 캡처 결과 (로컬 확인용)
/.screenshots/

# Cloudflare 배포 산출물
/.open-next/
/.wrangler/

# D1 덤프. 계정 이메일과 소셜 고유번호가 들어 있어 저장소에 두지 않는다.
*.sql.bak
backup-*.sql
d1-backup*.sql

# Claude Code 개인 설정. 권한 허용 규칙은 사람마다 다르므로 저장소에 두지 않는다
# (`.claude/settings.json`은 공유 설정이라 커밋한다).
.claude/settings.local.json
