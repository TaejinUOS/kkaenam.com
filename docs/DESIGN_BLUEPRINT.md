# 깨남.COM Visual Design Blueprint

| 항목 | 내용 |
| --- | --- |
| 문서 버전 | v0.6 |
| 기준 문서 | `PRD.md` v0.7 |
| 승인 상태 | **개발 시각 기준 확정 (2026-08-31)** |
| 최근 방향 변경 | **2026-09-01 — 카테고리 포스터 가로 확장 인터랙션과 서폿 Wtf?! 커버 추가** |
| 구현 반영 | **2026-09-01 — MVP 프런트엔드 구현. 구현 중 내린 결정은 12장 참조** |
| 콘셉트명 | **GLOWING MATCHUP ZINE — 야광 상대법 잡지** |
| 핵심 인상 | 게임 공략 잡지, PC방 스티커 보드, 형광펜 메모, 실험적인 한국어 에디토리얼 디자인 |

## 1. 디자인 선언

깨남.COM는 일반적인 게임 통계 대시보드처럼 보여서는 안 된다. 이 사이트는 사용자가 게임 직전에 펼쳐 보는 살아 있는 공략 잡지에 가깝다. 화면의 정보 구조는 빠르고 명확하게 유지하되, 비대칭 편집 그리드, 강한 한글 타이포그래피, 잘라 붙인 이미지, 형광펜 표시를 사용해 고유한 인상을 만든다.

다음 세 문장을 모든 디자인 판단의 기준으로 삼는다.

1. **챔피언은 카드 안의 썸네일이 아니라 한 장의 포스터다.**
2. **General은 인쇄된 본문이고, Me는 나중에 덧칠한 형광펜 메모다.**
3. **챔피언은 정적 2D 일러스트로 무대화하며, 모션은 정보 이해를 돕는 범위에서만 사용한다.**

### 1.1 확정된 개발 방향

사용자가 생성된 두 시각 목업과 이 문서의 디자인 방향을 승인했다. 이후 UI 구현과 확장은 다음 우선순위를 기본값으로 삼는다.

1. 기능 범위와 사용자 흐름은 `PRD.md`를 따른다.
2. 시각적 판단은 이 문서와 `design/blueprints/category-selection-v1.png`, `design/blueprints/ahri-matchup-v1.png`를 기준으로 삼는다.
3. 목업의 픽셀이나 임시 문구를 그대로 복제하기보다 비대칭 편집 구조, STATIC BLOOM 색 체계, 강한 한글 타이포그래피, 포스터·스티커·형광펜 문법과 정보 위계를 보존한다.
4. 새 화면과 컴포넌트도 같은 잡지의 다음 페이지처럼 느껴져야 하며, 범용 SaaS 대시보드나 둥근 카드 템플릿으로 회귀하지 않는다.
5. 반응형 사용성, 접근성, 성능, 실제 데이터와 에셋 사용 권리를 위해 필요한 조정은 허용하되 시각적 의도를 유지하고 의미 있는 변경은 이 문서에 기록한다.

생성 목업은 **시각 방향의 기준**이며 완성 콘텐츠나 배포 가능한 게임 에셋 자체는 아니다. 이미지 안의 임시 캐릭터 표현과 문구는 실제 구현 단계에서 검증된 데이터와 사용 가능한 에셋으로 교체한다.

## 2. 조사한 레퍼런스와 적용 방식

| 레퍼런스 | 참고할 점 | 그대로 가져오지 않을 점 |
| --- | --- | --- |
| [Godly](https://godly.design/) | 흥미로운 웹 인터랙션과 강한 시각적 첫인상 | 포트폴리오형 긴 인트로와 과도한 스크롤 연출 |
| [Land-book Brutalism](https://land-book.com/design/website/brutalism) | 굵은 선, 큰 타이포그래피, 비대칭 편집 구조 | 모든 요소를 거칠게 만드는 무분별한 네오브루탈리즘 |
| [Refs.Gallery Experimental](https://refs.gallery/collections/experimental-web-design) | 실험적인 조판과 시각적 리듬 | 사용성을 희생하는 해체적 내비게이션 |
| [Relume](https://www.relume.ai/) | 스타일 전에 사이트맵과 와이어프레임을 고정하는 작업 순서 | 컴포넌트 라이브러리의 기본 모양을 최종 디자인으로 사용 |
| [v0 Design Systems](https://v0.dev/docs/design-systems) | AI 생성 전에 색·글꼴·컴포넌트 토큰을 제공하는 방식 | 기본 `shadcn/ui` 외형을 수정 없이 사용 |
| [Coolors](https://coolors.co/) | 실제 UI 적용 전 색 조합과 대비를 함께 점검 | 인기 팔레트를 그대로 복제 |
| [Bagel Fat](https://noonnu.cc/en/font_page/1345) | 챔피언명에 어울리는 개성 있는 한글 디스플레이 서체 | 본문 전체에 적용해 가독성을 낮추는 사용 |

AI 도구는 레이아웃 초안과 변형 생성에만 사용한다. 최종 화면에서는 이 문서의 토큰과 컴포넌트 문법을 우선하며, AI가 자주 만드는 범용 SaaS 디자인으로 회귀하지 않게 한다.

## 3. 색상 시스템: STATIC BLOOM

보라색-파란색 그라디언트 대신, 검은 잉크 위에 종이와 형광 잉크를 얹은 색 체계를 사용한다.

| 토큰 | HEX | 용도 |
| --- | --- | --- |
| Ink | `#101014` | 기본 배경, 선, 본문 텍스트 |
| Paper | `#F4EBDD` | 읽기 영역, Tip 본문, 밝은 텍스트 |
| Acid | `#D8FF3E` | 현재 선택, 검색 포커스, 주요 CTA |
| Gum | `#FF5E9C` | `Me` 전용 내용, 개인화 상태 |
| Cobalt | `#3A4CFF` | `General`, 링크, 정보성 상태 |
| Smoke | `#A59BAD` | 보조 텍스트, 비활성 상태 |
| Alert | `#C81F26` | 읽지 않은 알림 배지. 이 용도 외에는 쓰지 않는다 |

권장 텍스트 조합:

- Paper on Ink: `16.07:1`
- Acid on Ink 또는 Ink on Acid: `16.54:1`
- Ink on Gum: `6.63:1`
- Paper on Cobalt: `4.86:1`
- Smoke on Ink: `7.13:1`
- Paper on Alert: `4.83:1`

Acid, Gum, Cobalt를 넓은 배경으로 동시에 사용하지 않는다. 한 화면에서 하나를 주 강조색으로 두고 나머지는 작은 레이블이나 상태 표현에 사용한다.

```css
:root {
  --ink: #101014;
  --paper: #f4ebdd;
  --acid: #d8ff3e;
  --gum: #ff5e9c;
  --cobalt: #3a4cff;
  --smoke: #a59bad;
  --line: 2px solid var(--ink);
  --radius-card: 2px;
  --radius-control: 999px;
  --shadow-cut: 6px 6px 0 var(--ink);
}
```

## 4. 타이포그래피

### Display

- **확정: `SB 어그로체 B`** (샌드박스네트워크, [눈누](https://noonnu.cc/font_page/738))
- 웹사이트·임베딩 모두 허용. 폰트 파일의 수정·복제·배포는 금지이므로 **서브셋하지 않고 원본 woff를 self-host**한다.
- 용도: 챔피언명, 화면 번호, 짧은 메인 카피
- 크기: 데스크톱 `clamp(72px, 10vw, 164px)`, 모바일 `clamp(52px, 18vw, 92px)`
- 한 화면에 최대 두 줄까지만 사용한다.
- 약간 겹치거나 잘릴 수 있지만 실제 HTML 텍스트로 유지한다.

### UI 및 본문

- 후보: `Pretendard Variable`
- 용도: 내비게이션, Tip 본문, 버튼, 설명
- 본문 기본 크기: 데스크톱 17px/1.65, 모바일 16px/1.6

### 수치 및 메타데이터

- 후보: `IBM Plex Mono`
- 용도: 쿨타임, 좋아요 수, 시즌, 작성일, 랭킹 번호
- 숫자가 게임 HUD처럼 정렬되도록 tabular numerals를 사용한다.

## 5. 형태와 질감

- 카드 모서리는 거의 직각으로 유지한다. 입력·필터처럼 반복 조작하는 컨트롤만 완전한 pill 형태를 허용한다.
- 선택 요소에는 흐린 그림자 대신 `6px` 하드 오프셋 그림자를 사용한다.
- 챔피언 이미지 위에는 저채도 하프톤, 인쇄 망점, 스캔 라인 중 한 종류만 약하게 적용한다.
- 섹션 번호는 `01 / 02 / 03`처럼 크게 노출해 공략 잡지의 목차 같은 리듬을 만든다.
- 경계선 일부는 스티커가 뜯긴 것처럼 한쪽 모서리를 `clip-path`로 잘라낸다.
- 장식용 별, 추상 그라디언트 구체, 의미 없는 3D 오브젝트는 사용하지 않는다.

## 6. 화면 Blueprint

### 6.1 글로벌 헤더

- 높이 72px, Ink 배경, Paper 텍스트, 하단 2px Paper 선
- 왼쪽에 `깨남.COM`, 중앙에 `상대법 / 전적 / 통계 / 티어표 / 강의`, 오른쪽에 마이페이지와 알림·로그아웃
- 알림은 획으로 그린 종 아이콘이고, 읽지 않은 알림이 있을 때만 오른쪽 위에 Alert 원과 Paper 숫자가 붙는다. 100 이상은 `99+`로 접는다.
- `전적`, `통계`, `티어표`에는 작은 모노 `SOON` 스티커를 붙이고 선택 시 추후 개발 안내 화면으로 이동한다.
- 메뉴 hover 시 페이지 전체를 흐리게 하지 않고, 콘텐츠에 8% Ink 오버레이를 씌우고 대상 메뉴만 Acid로 반전한다.
- 현재 메뉴는 밑줄 대신 Acid 직사각형 탭으로 표시한다.

### 6.2 포지션·카테고리 선택 화면

- 상단 카피 `누굴 상대해?` 아래에 12~24px의 반응형 간격을 두고 `탑 / 정글 / 미드 / 원딜 / 서폿`을 종이 인덱스 탭처럼 배치하며, 현재 포지션은 Acid 면으로 표시한다.
- URL에 포지션이 없을 때는 `미드`를 활성화하고, 우측 메타데이터의 첫 항목도 `MID`처럼 현재 포지션에 맞춰 변경한다.
- 카테고리 포스터 수에 따라 데스크톱 12열을 다르게 사용한다. 3개는 `5 / 4 / 3`, 2개는 `7 / 5`, 1개는 8열 포스터와 4열 설명 영역으로 구성한다.
- 포스터 수가 달라도 동일한 둥근 카드 그리드로 만들지 않으며 각 포스터의 카테고리명은 이미지 바깥으로 8~16px 튀어나오게 배치한다.
- 대표 이미지와 카테고리의 정확한 매핑은 `PRD.md` 5.1을 따른다. 미드의 Zed·Zoe·yone 목업은 다른 포지션에도 적용할 조판 문법의 기준이다.
- 포지션 전환 시 포스터 세트가 교체되고, 이전 Contact Sheet와 검색 결과는 남기지 않는다.

포인터 호버 또는 키보드 포커스 시 해당 포스터는 같은 포스터 그룹 안에서 가로 폭이 확장되고 나머지 포스터는 축소된다. 전체 그룹 너비는 유지하며 축소된 포스터의 이미지는 시각적으로 잘릴 수 있지만 카테고리명과 선택 영역은 유지한다. 포인터와 포커스가 벗어나면 원래 비대칭 배치로 복원한다.

가로 확장을 주 동작으로 사용하되 이미지의 `2deg` 이내 기울기, 하드 그림자의 6px→12px 증가와 라벨 8px 이동을 보조 피드백으로 함께 사용할 수 있다. 포스터가 하나뿐인 원딜에서는 폭 변화 없이 이 보조 피드백만 사용한다. 터치 환경에서는 포스터 폭 변화를 선택에 필요한 전제 조건으로 삼지 않는다.

카테고리 선택 후에는 같은 페이지 아래에 **챔피언 Contact Sheet**를 펼친다.

- Contact Sheet는 큰 카드 갤러리가 아니라 포스터 아래에 붙는 조밀한 필름 스트립으로 취급한다. 데스크톱(1200px 이상)은 12~13열, 태블릿은 8열, 모바일은 4열을 사용한다.
- 데스크톱 아이콘은 가용 폭에 따라 약 84~96px 범위에서만 커지며, 목록이 적거나 화면이 더 넓어져도 남은 폭을 나눠 가지며 대형 카드로 늘어나지 않는다.
- 모든 아이콘은 1:1 비율을 유지하고, 6~8px의 좁은 간격과 2px Paper 외곽선·1px Ink 내부선을 사용한다. 프레임 모서리는 작게 대각선으로 잘라 인쇄물 Contact Sheet의 재단 문법을 따른다.
- 챔피언명은 화면에 표시하지 않는다. Contact Sheet는 아트만 늘어놓은 필름 스트립으로 읽혀야 하고, 이름은 위쪽 검색창과 스크린리더 전용 텍스트가 맡는다.
- 포인터 호버와 키보드 포커스는 라벨 면 전체를 Acid로 채우지 않고, 잘린 프레임을 따라가는 Acid 윤곽선과 최대 1.04배 확대를 함께 사용한다.
- 검색 결과가 줄어들 때 아이콘의 위치가 바뀌되 과도한 spring 애니메이션은 사용하지 않는다.

### 6.3 챔피언 상대법 화면

데스크톱에서는 Wiki 가독성을 우선해 기존 `Aside 5열 + Main 7열`에서 Aside 폭을 2/3로 줄인
`Aside 약 3.3열 + Main 약 8.7열`을 사용한다. 모바일에서는 `Aside → Main` 순서로 쌓는다.

#### Aside

- Ink 배경 위에 Paper 격자 또는 인쇄 재단선 패턴을 깐다.
- 정적 2D 챔피언 일러스트는 화면 중앙이 아니라 오른쪽으로 8% 치우치게 배치한다.
- 챔피언명은 일러스트 뒤에서 일부 가려지는 초대형 Gum 텍스트로 표시하되, 읽을 수 있는 작은 이름 레이블을 별도로 제공한다.
- Q/W/E/R은 2×2 또는 가로 4개 배열로 두고, 각 아이콘 우측 상단에 모노 글꼴로 쿨타임을 표시한다.
- 스킬 상세는 일반 툴팁 대신 Paper 색의 작은 **공략 쪽지**처럼 펼쳐진다.

#### Main

- `게시판 / 영상`은 밑줄 탭이 아니라 종이 인덱스 탭처럼 상단 테두리에 붙인다.
- 게시판 첫 줄에는 `ME / 내 챔피언 선택` 콤보박스와 `좋아요순` 정렬을 둔다.
- Tip은 둥근 카드 대신 얇은 가로 기사 형태로 쌓는다.
- 왼쪽 56px에는 랭킹 번호, 가운데에는 제목과 General, 오른쪽에는 작성자·평가 메타데이터를 배치한다.
- Tip의 제목은 최대 2줄, 본문은 최대 3줄로 제한한다. `Me` 미선택 시 General 3줄, 선택 시 General 2줄과 Me 1줄을 사용하며 메타데이터와 레이블은 이 줄 수에서 제외한다.
- General은 Paper 바탕에 Cobalt 라벨을 사용한다.
- Me가 선택되면 해당 전용 문장이 Gum 배경의 형광펜 획처럼 Tip 내부에 추가된다.
- Me가 없는 Tip은 높이가 바뀌지 않도록 빈 영역을 예약하지 않는다.

### 6.4 영상 화면

- 게시판과 동일한 프레임 안에서 2열 영상 목록을 제공한다.
- 썸네일을 둥근 영상 카드로 만들지 않고, 16:9 이미지 위에 Paper 제목 띠를 겹친다.
- 재생 시간과 패치 버전은 Cobalt 모노 레이블로 표시한다.

### 6.5 추후 개발 안내 화면

- `전적`, `통계`, `티어표`가 같은 프레임을 공유하되 선택한 메뉴명을 초대형 디스플레이 타이포그래피로 표시한다.
- Acid 또는 Gum 스티커에 `추후 개발 / COMING SOON`을 함께 표시하고 `상대법으로 돌아가기` 동작을 제공한다.
- 구현된 것처럼 보이는 가짜 검색창, 통계 숫자, 차트와 티어 카드는 배치하지 않는다.

## 7. 모션 문법

| 상황 | 모션 | 권장 시간 |
| --- | --- | --- |
| 포지션 전환 | 활성 인덱스 반전 후 포스터 세트를 짧은 잉크 마스크로 교체 | 220ms |
| 카테고리 포인터·포커스 | 대상 포스터 가로 확장 + 나머지 축소, 보조로 `2deg` 이내 회전과 하드 그림자 증가 | 160ms |
| 카테고리 선택 | 라벨이 화면 상단 인덱스로 이동하며 Contact Sheet 공개 | 280ms |
| 챔피언 선택 | 아이콘 1.04배 확대 후 중앙에서 번지는 잉크 마스크 전환 | 220ms |
| 챔피언 일러스트 공개 | 이미지 자체는 정적으로 유지하고 최초 진입 시 잉크 마스크로 한 번만 공개 | 220ms |
| Me 선택 | 일치하는 문장에 왼쪽에서 오른쪽으로 형광펜 획 공개 | 320ms |
| 좋아요 | 숫자 한 자리만 위로 넘기는 odometer 효과 | 180ms |

`prefers-reduced-motion`에서는 잉크 마스크와 위치 이동을 제거하고 색상·선·즉시 공개로 상태를 표현한다.

## 8. 반응형 규칙

- 1440px 이상: 콘텐츠 최대 폭 1600px, 12열
- 1024~1439px: 12열 유지, Aside 약 3.1열 + Main 약 8.9열
- 768~1023px: 8열, 챔피언 Aside를 상단 가로형으로 변경
- 360~767px: 4열, 카테고리 포스터를 세로로 쌓고 2D 일러스트 영역 높이를 320px 이하로 제한
- 모바일 포지션 인덱스는 한 줄 가로 스크롤로 제공하고 현재 탭이 처음부터 보이도록 스크롤 위치를 맞춘다.
- 가로 포스터 확장은 포인터 호버를 지원하는 정밀 포인터 환경과 키보드 포커스에서 제공한다. 터치 환경에서는 폭 변화 없이 탭 선택 피드백을 제공한다.
- 모바일 스킬 설명은 hover 툴팁이 아니라 하단 sheet로 표시한다.
- 모바일에서 `Me` 콤보박스와 정렬 컨트롤은 게시판 상단에 sticky로 유지한다.

## 9. AI 티를 방지하는 금지 목록

- 보라색·파란색 mesh gradient 배경
- 모든 요소에 16~24px radius를 적용한 카드 대시보드
- 반투명 glassmorphism 패널과 blur 남용
- 아이콘 + 제목 + 두 줄 설명이 반복되는 3열 feature grid
- 의미 없는 별빛, 빛나는 구체, 추상 3D 링
- 모든 여백을 동일하게 맞춘 지나치게 안전한 중앙 정렬
- 화면마다 다른 임의의 색과 border radius
- 브랜드 이유 없이 Inter 한 종류만 사용하는 타이포그래피
- 기능과 관계없는 scroll hijacking 및 긴 로딩 인트로

## 10. 구현 전 승인 체크리스트

- [x] 카테고리 화면과 아리 상대법 화면의 데스크톱 시각 방향 승인 (2026-08-31)
- [x] STATIC BLOOM 팔레트와 GLOWING MATCHUP ZINE 방향 승인 (2026-08-31)
- [x] 디스플레이 서체와 본문 서체의 실제 한글 조합 확인 (2026-09-01, SB 어그로체 B + Pretendard)
- [ ] General과 Me의 색·레이블 구분 확인
- [ ] 정적 2D 일러스트의 데스크톱·모바일 크롭과 로딩 실패 대체 UI 확인
- [ ] 다섯 포지션과 12개 카테고리 커버의 비대칭 포스터 조합 확인
- [ ] 카테고리 포스터 가로 확장·복원, 축소 포스터 레이블, 키보드 포커스와 애니메이션 감소 환경 확인
- [ ] 전적·통계·티어표 추후 개발 안내 화면과 모바일 메뉴 확인
- [ ] 모바일 360px 화면에서 검색, 스킬, Me 필터 사용성 확인
- [ ] 색상 대비와 키보드 포커스 확인
- [ ] Riot 에셋과 서체의 사용 조건 확인

## 11. 생성된 시각 목업

### 카테고리 선택 화면

![깨남.COM 카테고리 선택 화면 목업](./design/blueprints/category-selection-v1.png)

- Zed, Zoe, Yone 원본 이미지를 각각 미드의 암살자, 메이지, 브루저/원딜 포스터 레퍼런스로 사용했다.
- 비대칭 포스터 비율, 겹치는 스티커 라벨, Contact Sheet가 핵심 검토 대상이다.
- 이 목업은 미드가 선택된 상태를 나타내며, 다른 포지션도 같은 조판 문법과 `PRD.md` 5.1의 커버 매핑을 사용한다.

### 아리 상대법 화면

![깨남.COM 아리 상대법 화면 목업](./design/blueprints/ahri-matchup-v1.png)

- 생성 당시의 5열 Aside, 7열 Main과 Q/W/E/R, 스킬 쪽지, 좋아요순 게시판, General과 Me의 관계를 한 화면에 표현했다. 실제 구현의 현재 열 비율은 6.3의 가독성 조정 규칙을 따른다.
- 목업 속 챔피언 일러스트와 문구는 시각 방향 확인용이다. 실제 구현에서는 권리 확인된 정적 2D 일러스트와 Data Dragon 데이터를 사용한다.

두 이미지는 Codex의 내장 ImageGen 경로로 생성했다. 재생성·변형에 사용한 전체 프롬프트는 [`design/blueprints/imagegen-prompts.md`](./design/blueprints/imagegen-prompts.md)에 보관한다.

## 12. 구현에서 내린 결정 (2026-09-01)

MVP 프런트엔드를 구현하며 목업과 달라지거나 문서에 없던 판단을 내린 항목을 기록한다. 모두 1.1의 우선순위 — 반응형 사용성, 접근성, 성능, 실제 데이터 정확성, 에셋 사용 권리 — 를 목업 재현보다 앞세운 결과다.

| 항목 | 결정 | 이유 |
| --- | --- | --- |
| Aside 일러스트 소스 | Data Dragon `loading` 아트(308×560)를 사용하고, 챔피언별 `object-position`으로 초점을 지정한다 | 세로 구도가 Aside 조판과 맞고 Riot이 배포를 허용한 경로라 사용 권리가 확인된다. `splash`는 가로 구도라 6.3의 구성과 어긋난다 |
| 스킬 쪽지 위치 | 떠 있는 툴팁이 아니라 스킬 줄 바로 아래에 Aside 폭으로 펼친다 | PRD 9의 "화면 밖으로 잘리지 않는다"를 어떤 화면 폭에서도 보장하기 위해서다. 종이 쪽지라는 인상은 유지했다 |
| 포스터 가로 확장 | CSS `:has()` + `flex-grow`로 구현하고 `(hover: hover) and (pointer: fine)`에서만 적용한다. 포스터가 하나인 원딜은 폭 변화에서 제외한다 | 8장의 터치 규칙을 지키면서 그룹 전체 폭을 유지하기 위해서다 |
| 헤더 메뉴 구성 | `상대법 / 전적 / 통계 / 티어표 / 강의` + 마이페이지. `상대법`을 뺀 전부가 공통 `추후 개발` 프레임으로 간다 | 6.1의 메뉴 구성과 PRD 5.7의 안내 화면 요구를 합친 결과다 |
| 영상 탭 | 임시 썸네일을 만들지 않고 빈 상태만 제공한다. 6.4의 2열 골격은 자리만 잡아 둔다 | PRD 3.2가 가짜 데이터를 금지하고, 영상 출처·검수 주체는 PRD 15의 미결정 8번이다 |
| 서체 | IBM Plex Mono는 `next/font/google`, 디스플레이 서체는 `next/font/local`, Pretendard는 배포 CDN의 동적 서브셋으로 불러온다 | Pretendard와 디스플레이 서체가 Google Fonts에 없다 |
| **디스플레이 서체 (4장 변경)** | Bagel Fat One에서 **SB 어그로체 B**로 바꾼다 | Bagel Fat One은 목업보다 자폭이 좁고 속공간이 둥글어 말랑해 보였다. 어그로체가 목업의 두껍고 각진 인상에 더 가깝다. 1순위였던 쿠키런은 라이선스가 **게임산업군 사용을 상업·비상업 모두 금지**해 제외했다 |
| 스킬 데이터 | 목업의 스킬 배치를 따르지 않고 Data Dragon `16.17.1` `ko_KR`을 그대로 쓴다 | 목업은 아리의 매혹을 W로 표시했지만 실제로는 E다. 목업은 구도 기준이지 데이터 기준이 아니다 (AGENTS.md) |
| **챔피언 아트 위 하프톤 (5장 변경)** | 커버·아이콘·Aside 일러스트에서 망점 오버레이를 걷어내고, 대신 채도와 대비를 올린다(`--art-punch`) | 5장은 이미지 위에 하프톤을 약하게 깔라고 했지만, 실제 화면에서 3px 망점이 곱하기로 겹쳐 목업보다 탁하고 흐릿했다. 인쇄물 질감은 지면 배경과 Aside 재단선 격자가 계속 담당한다 |
| Aside 일러스트 왼쪽 가장자리 | 잉크 배경으로 녹아들도록 마스크를 건다 | 목업 챔피언은 배경 없는 컷아웃이라 뒤의 초대형 이름이 비쳤지만, Data Dragon 일러스트는 배경이 있는 사각형이라 이름을 통째로 가렸다 |
| **상대법 화면 열 비율 (6.3·8장 변경)** | 데스크톱 Aside를 기존 폭의 2/3로 줄이고 확보한 폭을 Wiki Main에 배정한다 | 문서가 화면의 핵심 콘텐츠인 만큼 긴 공략의 줄바꿈을 줄이고 가독성을 높이기 위해서다. 1023px 이하의 상하 배치는 유지한다 |
| **위키 장문 가독성 보강 (2026-09-03)** | Main 본문 열에 문서 제목을 두고, 본문은 최대 52rem·16px로 제한한다. 문단 사이는 14px, 제목 단계는 23/19/17/16px로 분리한다 | 실서비스 측정에서 15px 본문이 1004px까지 늘어나고 Markdown wrapper 안의 문단 간격이 0px인 것을 확인했다. 잡지 프레임은 유지하되 읽기 면은 차분한 문서 조판을 우선한다 |
| **선택한 Me 표시 (2026-09-03)** | `me / 챔피언명` Gum 스티커는 사용하지 않고, 현재 선택한 섹션 제목에 Paper Shade 배경·Cobalt 왼쪽 선·`현재 선택` 텍스트를 함께 쓴다 | 스티커를 반복하지 않으면서도 PRD 5.3.3의 색 이외 단서와 현재 위치 확인을 충족한다 |
| Tip 작성 계정 | 브라우저에만 저장되는 로컬 데모 계정으로 작성 흐름을 제공하고, 화면에 로컬 저장임을 명시한다 | PRD 15의 미결정 1번(회원가입 방식)이 정해지기 전까지의 임시 조치다 |
| **헤더 오른쪽 구성과 Alert 토큰 (3·6.1장 변경, 2026-09-06)** | 헤더에서 닉네임·`내 편집`·`관리자` 링크를 걷어내고 그 자리에 알림 종을 둔다. 배지 색으로 팔레트에 없던 `--alert`(#C81F26)를 새로 세운다 | 걷어낸 셋은 전부 마이페이지에 있어 헤더가 같은 것을 두 벌로 들고 있었고, 헤더에만 있을 수 있는 것은 **아직 열어 보지 않은 소식**이다. Gum은 `Me` 전용 개인화 색이라 알림에 쓰면 한 색이 두 뜻을 갖는다. 밝은 빨강에서는 11px 숫자의 Paper 대비가 4.29:1로 기준에 못 미쳐 한 단계 어둡게 잡았다 |

### 남은 시각 확인

10장 체크리스트 중 다음 항목은 구현은 끝났지만 **실제 브라우저에서 눈으로 확인하는 단계가 남아 있다.**

- 긴 카테고리명 `브루저/원딜`의 포스터 라벨이 좁아진 포스터에서 깨지지 않는지
- 다섯 포지션 12개 카테고리에서의 비대칭 포스터 조합과 축소 포스터 레이블
- 데스크톱·모바일 일러스트 크롭과 360px 화면의 검색·스킬·Me 필터 사용성
- 색상 대비와 키보드 포커스
