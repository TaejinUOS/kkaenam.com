# 깨남.COM 인수인계 — 남은 작업과 알아 두어야 할 것

| 항목 | 내용 |
| --- | --- |
| 기준일 | 2026-09-04 (병합 편집 화면과 "지운 것이 있는가" 판정 반영) |
| 대상 | `docs/PRD.md` v1.0 / `docs/DESIGN_BLUEPRINT.md` v0.6 / `docs/WIKI_MODEL.md` |
| 구현 범위 | MVP 프런트엔드 (Next.js 15 App Router, TypeScript) |
| Data Dragon | `16.17.1` / `ko_KR` — 챔피언 173명, 분류 166명 |

이 문서는 **지금 무엇이 임시이고, 무엇이 없고, 무엇을 결정해야 하는지**를 모아 둔 곳이다.
기능 명세는 `PRD.md`, 시각 기준은 `DESIGN_BLUEPRINT.md`가 담당한다.

Riot 제품 심사·수익화 조건과 신청 준비 목표는 [RIOT_APPROVAL.md](./RIOT_APPROVAL.md)에 정리했다
(2026-09-05). 실제 포털 등록·승인·키 보유 상태는 미확인이다. 현재 위키의 등록·광고 모델
검토를 우선 준비하고, 전적 기능은 PRD의 후속 범위로 둔다.

---

## 1. 한눈에 보기

| 구분 | 상태 |
| --- | --- |
| PRD 7장 P0 중 선택·탐색 (FR-01 ~ FR-10, 12, 13, 18 ~ 20) | 구현 완료 |
| FR-23 위키 문서 열람 | 구현 완료 (D1) |
| FR-22 로그인 | **구현 완료** (2026-09-03). 구글·카카오, JWT 세션 |
| FR-24 ~ 33 편집·검토 | **구현 완료** (2026-09-03). 아래 2.1 참고 — **실제 로그인 상태 브라우저 클릭 검증은 미완료** |
| FR-52 ~ 57 일반 문서 (위키 확장 0~2단계) | **구현 완료** (2026-09-05). `/wiki/<이름>` 열람·편집·역사, `/wiki/new` 제안, 운영자 승인. 설계는 `docs/WIKI_EXPANSION.md`. **쓰기 흐름(제안·승인·거절)의 로그인 브라우저 검증은 미완료** — 로컬은 소셜 로그인만 있어 확인하지 못했다 |
| FR-34 ~ 43 관리자 챔피언 분류 | **대부분 구현.** 배정(FR-34·35·36·42) + 표시 순서(FR-38) + 운영 상태(FR-39). 남은 것은 FR-40·41(동기화 검토 상태)과 FR-43(변경 이력). FR-37은 폐기 — 4장 |
| FR-58 알림 | **구현 완료** (2026-09-06). 헤더 종 + `/my/notifications`. 알림 표를 만들지 않고 `wiki_edits`에서 만들어 낸다 — 저장하는 것은 `users.notifications_read_at` 하나뿐이다 (마이그레이션 0009). 운영 DB에도 적용 완료(2026-09-06) |
| FR-59 문서 내리기 | **구현 완료** (2026-09-06). 운영자가 일반 문서를 내린다. 행을 지우지 않고 `doc_status='deleted'` + `title_key=NULL` + 링크 삭제 (마이그레이션 0010). 되돌리기는 `/admin/wiki/deleted` |
| FR-21 신고 및 관리 (P1) | 미구현 |
| 서버 · 데이터베이스 | Cloudflare Workers + D1. 배포 운영 중 |
| 인증 | 구글·카카오 소셜 로그인, JWT 세션(Adapter 미사용). `role`로 검토·관리자 권한 게이팅 (2026-09-03 연동) |
| 편집 기능 | **구현 완료** (2026-09-03). 섹션 단위 제안·즉시반영/검토·역사·되돌리기·최근 변경·제출 제한. 마크다운 지원 |
| 영상 탭 | **빈 상태만.** 콘텐츠 없음 |
| 전적 · 통계 · 티어표 · 강의 · 마이페이지 | `추후 개발` 안내 화면 (PRD 5.7대로 의도된 상태) |
| 타입체크 · 린트 · 프로덕션 빌드 | 통과 |
| 실제 브라우저 시각 검수 | 데스크톱·모바일 360px 확인 완료 (2026-09-02) — 6장 참조 |

---

## 2. 임시 구현 — 실서비스 전 반드시 교체

### 2.1 편집·검토 (4단계, 2026-09-03 구현 완료)

**지금 상태.** 섹션 단위 편집 제안, 즉시반영/검토 판정, 검토 대기열, 문서 역사,
되돌리기, 최근 변경, 제출 제한(FR-24~33)을 모두 구현했다. 설계는
[`docs/WIKI_MODEL.md`](./WIKI_MODEL.md), 계획은 세션 진행 중 승인받은 계획 문서를 따랐다.

**핵심 파일.**

| 영역 | 파일 |
| --- | --- |
| 쓰기 계층 (D1) | [`src/lib/wikiEditStore.ts`](../src/lib/wikiEditStore.ts) — 제출·승인·거절·되돌리기·역사·최근변경·내편집 |
| 서버 액션 | [`src/lib/actions/wikiEditActions.ts`](../src/lib/actions/wikiEditActions.ts) |
| 권한 게이팅 | [`src/lib/authGuard.ts`](../src/lib/authGuard.ts) — `requirePageAdmin`/`requirePageUser`(페이지), `requireActionAdmin`/`requireActionUser`(액션) |
| 편집 화면 | [`src/app/matchup/[position]/[champion]/edit/page.tsx`](../src/app/matchup/[position]/[champion]/edit/page.tsx) + [`MergeEditScreen.tsx`](../src/components/matchup/MergeEditScreen.tsx) — `?section=general` / `?section=me:<슬러그>`. 문서 화면 안 인라인 패널이 아니라 별도 주소다 |
| 편집 진입점 | [`src/components/matchup/WikiPanel.tsx`](../src/components/matchup/WikiPanel.tsx) — 섹션 제목 줄의 `편집` 링크가 위 주소로 보낸다 |
| 차이 계산 | [`src/lib/wikiDiff.ts`](../src/lib/wikiDiff.ts) — **클라이언트와 서버가 함께 쓴다.** `server-only`를 넣지 말 것 |
| 형광펜 렌더 | [`src/components/wiki/DiffText.tsx`](../src/components/wiki/DiffText.tsx) — 읽기 전용(좌측 면). 편집 화면과 검토 화면이 함께 쓴다 |
| 형광펜 입력칸 | [`src/components/wiki/HighlightedEditor.tsx`](../src/components/wiki/HighlightedEditor.tsx) — 사본과 textarea의 치수를 맞추는 까다로운 부분이 여기 모여 있다. **두 화면이 한 벌을 쓴다** |
| 검토 화면 | [`src/components/admin/ReviewMergeForm.tsx`](../src/components/admin/ReviewMergeForm.tsx) — 오른쪽 면이 곧 `반영할 내용` 입력칸이다. 형광펜 기준은 제안 원문이 아니라 지금 문서 |
| 마크다운 렌더러 | [`src/components/wiki/MarkdownBody.tsx`](../src/components/wiki/MarkdownBody.tsx) — `markdown-to-jsx`, `disableParsingRawHTML: true` |
| 관리자 화면 | `src/app/admin/**` (`/admin`, `/admin/wiki/review`, `/admin/wiki/review/[editId]`, `/admin/wiki/recent`) |
| 문서 역사(공개) | `src/app/matchup/[position]/[champion]/history/page.tsx` |
| 내 편집 | `src/app/my/edits/page.tsx` |

**핵심 분기 로직** (`wikiEditStore.ts`의 `submitEdit`):

```
isDelete = isEmptyBody(제출 본문)
wasEmpty = isEmptyBody(현재 실제 값)   // 저장 시점에 서버가 새로 읽음
accept      = isAdmin || (!isDelete && isAdditionOnly(현재 실제 값, 제출 본문))
acceptedVia = !accept ? null : isAdmin ? "admin" : wasEmpty ? "empty_section" : "addition_only"
```

**기준은 "섹션이 비었는가"가 아니라 "이 편집이 지우는 글자가 있는가"다** (2026-09-04
개정, WIKI_MODEL.md "편집에는 두 갈래가 있다"). 남의 글을 그대로 두고 덧붙이기만 하면
이미 쓰인 섹션이라도 즉시 반영된다. 공백·줄바꿈만 달라진 것은 지운 것으로 세지 않는다.

`isAdditionOnly`는 편집 화면의 빨간 형광펜과 **같은 함수**다. 둘이 갈리면 화면에
초록만 보이는데 대기열로 가는 일이 생기므로, `wikiDiff.ts`를 고칠 때는 양쪽 영향을
함께 본다.

관리자 본인 편집은 삭제라도 즉시 반영된다(자기 글을 자기가 검토하지 않음). 빈 본문
제출은 섹션 전체를 지우는 것이라 항상 검토 대기다(FR-26).

동시 제출 경쟁은 **`wiki_docs.revision`을 먼저 올려 문서를 쥐는 방식**으로 막는다.
`UPDATE ... WHERE revision = 읽은 값`이 0건이면 그 사이 누군가 반영했다는 뜻이므로,
바뀐 본문을 다시 읽어 다시 판정한다(세 번까지). 앞사람 글을 지우게 되는 편집이라면
그제야 검토 대기로 간다.

**되돌리기(FR-30)는 문서 전체가 아니라 섹션별로 재구성한다** —
`wiki_edits.revision`이 섹션 하나가 바뀔 때마다 1씩 오르는 카운터라 문서 전체
스냅샷 번호가 아니기 때문이다. 되돌리기 한 번이 여러 개의 새 리비전 번호를 만들 수
있다 — 의도된 동작이다.

**마크다운 지원 확정** (PRD 15 미결정 5번 해소). 섹션 본문 최대 4,000자, 편집 요약
선택 입력 최대 80자, 시간당 제출 상한 10건(미결정 6·7번 해소)으로 정했다.
`markdown-to-jsx`를 `disableParsingRawHTML: true` + href `http(s)` 화이트리스트로
써서 `dangerouslySetInnerHTML`을 전혀 쓰지 않는다 — 스모크 테스트로 `<script>`,
`onerror`, `javascript:`, `data:` 벡터가 무해하게 렌더됨을 확인했다.

**검증한 것.** 타입체크·린트·`next build`·`opennextjs-cloudflare build` 통과.
로컬 D1에 실제 SQL을 재현해 분기·원자성·되돌리기 재구성·제출 제한 카운트를
직접 확인했다. Auth.js JWT를 로컬에서 직접 서명해(`@auth/core/jwt`의 `encode`,
`AUTH_SECRET` 사용) 회원/관리자 세션 쿠키를 만들고, 서버 사이드 게이팅
(`/admin`·`/my/edits` 리다이렉트·404)과 FR-24 안내 문구가 역할·섹션 상태에 따라
정확히 갈리는 것을 실제 HTTP 요청으로 확인했다.

**검증하지 못한 것 — 다음에 브라우저로 직접 확인할 것.** 실제 구글·카카오 OAuth
로그인 상태에서 편집 저장 → 즉시반영/검토대기 화면 전환, 검토 승인·거절 버튼 클릭,
되돌리기 2단계 확인 UI, 대기열·최근변경·역사 화면의 실제 클릭 흐름은 이 세션에서
브라우저 확장이 연결되지 않아 눈으로 보지 못했다. 서버 로직과 데이터 계층은
검증했지만, React 폼 제출 → 서버 액션 호출의 실제 클릭 경험은 미확인이다.

**3단계 결론 (2026-09-03, 로그인).** Auth.js(next-auth v5) 공식 D1 어댑터는 이 스택
(`@opennextjs/cloudflare` + Cloudflare Workers)에서 알려진 버그가 있다
(`opennextjs/opennextjs-cloudflare#435` — 로컬 개발 중 D1 어댑터가 세션·계정을 저장하려
할 때 `getCloudflareContext` 관련 500 에러). 그래서 Adapter 인터페이스는 쓰지 않고,
**JWT 세션 전략** + `jwt` 콜백에서 [`src/lib/userStore.ts`](../src/lib/userStore.ts)로
D1에 직접 upsert하는 방식으로 우회했다. `src/auth.ts`가 설정 전체를 담고 있다.

**같은 사람이 구글·카카오 각각으로 로그인하면 서로 다른 계정이 된다** — `WIKI_MODEL.md`가
정한 대로이고 이번 구현도 그대로 따랐다. 자동 병합은 하지 않는다.

**시크릿 관리.** 로컬은 `.dev.vars`(gitignore 대상, `.dev.vars.example`을 복사해 채운다),
운영은 `npx wrangler secret put <NAME>`으로 등록한다(`AUTH_SECRET`,
`AUTH_GOOGLE_ID`/`SECRET`, `AUTH_KAKAO_ID`/`SECRET` 5개). 값은 저장소에 커밋하지 않는다.

**관리자 승격은 아직 화면이 없다.** `users.role`을 `admin`으로 올리려면 지금은 수동으로
직접 실행해야 한다 (FR-34 관리자 접근 제어 화면이 붙기 전까지의 임시 절차).

```bash
npx wrangler d1 execute kkaenam-gg --remote --command \
  "UPDATE users SET role='admin' WHERE id='...';"
```

> **즉시 반영 판정은 서버에서** 한다(`submitEdit`이 저장 시점에 실제 값을 다시 읽어
> `isAdditionOnly`로 비교한다). 지운 것이 없으면 바로 반영되고, 지우거나 고쳤으면
> 검토를 거치며, 섹션을 비우는 편집은 언제나 검토 대기다. PRD 5.4.1과 10장,
> WIKI_MODEL.md대로 구현했다.

> 즉시 반영 경로의 안전장치인 **최근 변경 화면, 한 번에 되돌리기, 제출 수 제한**
> 셋 다 구현했다(2.1 참고). 실제 서비스에 배포하기 전에 셋이 모두 동작하는지
> 다시 한 번 확인할 것. **판정 기준을 바꾸면서 즉시 반영되는 면이 크게 넓어졌다** —
> 예전에는 빈 섹션만 열려 있었지만 이제 모든 섹션이 덧붙이기에 열려 있다.
> 최근 변경 피드를 실제로 훑는 운영이 이 정책의 전제다.

### 2.2 영상 탭

`영상` 탭은 **빈 상태 안내만** 보여 준다. 가짜 썸네일이나 임시 영상은 넣지 않았다.
PRD 3.2가 가짜 데이터를 금지하고, 영상의 출처·임베드 허용 범위·등록 검수 주체가
PRD 15 미결정 8번으로 남아 있기 때문이다.

블루프린트 6.4의 2열 골격은 자리만 잡아 두었으니, 출처가 정해지면
[`src/components/matchup/VideoPanel.tsx`](../src/components/matchup/VideoPanel.tsx)에
목록을 채우면 된다. 데이터 형태는 PRD 8장 "영상" 항목을 따른다.

### 2.3 섹션 길이 제한과 본문 형식 (2026-09-03 확정, PRD 15 미결정 5~7번 해소)

| 항목 | 값 |
| --- | --- |
| 섹션 내용 최대 길이 | 4,000자 (`MAX_BODY_LENGTH`, `src/data/wiki.ts`) |
| 편집 요약 | 선택 입력, 최대 80자 (`MAX_SUMMARY_LENGTH`) |
| 계정당 시간당 제출 수 상한 | 10건 (`RATE_LIMIT_PER_HOUR`) |
| 마크다운 지원 | 지원함 (`markdown-to-jsx`, `MarkdownBody.tsx`) |
| 위키 전용 문법 | 각주 `[* 내용]`, 문서 링크 `[[문서명]]` (`src/lib/wikiMarkup.ts`) |

**각주 문법을 한 번 바꿨고 운영 데이터를 이관했다 (2026-09-03).** 처음에는 글 아래에
정의를 모으는 방식(`[^가]` + `[^가]: 내용`)이었으나 `[* 내용]`으로 바꿨다. 이유는
`WIKI_MODEL.md`의 "주석은 내용을 그 자리에 적는다"에 적었다.

이관은 운영 D1에서 아래 한 번으로 끝냈다. 닫는 `]`는 그대로 쓰이므로 여는 쪽만 바꾼다.

```sql
UPDATE wiki_docs     SET general = REPLACE(general, '[^', '[* ') WHERE general LIKE '%[^%';
UPDATE wiki_sections SET body    = REPLACE(body,    '[^', '[* ') WHERE body    LIKE '%[^%';
UPDATE wiki_edits    SET body    = REPLACE(body,    '[^', '[* ') WHERE body    LIKE '%[^%';
```

6행(문서 1 + 편집 이력 5)이 바뀌었고 옛 문법은 0행이 남았다. **편집 이력까지 함께 바꾼
것은 의도한 선택**이다 — 시험 삼아 쓴 글이라 기록 보존 가치보다 역사 화면이 깨져
보이지 않는 쪽이 낫다고 판단했다. 실제 기여가 쌓인 뒤라면 이 판단은 달라져야 한다.

**시드 이관분 재이관은 하지 않았다.** 옛 Tip 제목이 본문 앞 줄에 평문으로 남아 있는
문서들은 마크다운 제목 문법(`#`)으로 바꾸지 않아도 그대로 문단으로 정상 렌더된다 —
필요하면 나중에 별도 스크립트로 정리해도 되는 선택 사항이지 4단계를 막는 일은 아니었다.

---

## 3. 미구현 기능

| 기능 | 근거 | 비고 |
| --- | --- | --- |
| 관리자 챔피언 분류 | PRD FR-34 ~ 43 | **v0.8에서 범위 복귀.** 로그인·관리자 게이팅은 완료 — 4장 |
| 신고 및 관리자 처리 | PRD FR-21 (P1) | MVP 안정화 이후 |
| 문서별 토론 | PRD 13 향후 확장 | 스키마를 건드리지 않고 표만 추가하면 된다 |
| 프로필 수정 | — | `마이페이지`는 안내 화면만 |
| 강의와 결제 | `archive/plan1.txt` | `강의`는 안내 화면만 |
| 전적 · 통계 · 티어표 | PRD 3.2 비목표 | **의도적으로 안내 화면만.** 가짜 수치 금지 |
| 북마크 · 최근 본 챔피언 | PRD 13 향후 확장 | — |

`전적`·`통계`·`티어표`·`강의`·`마이페이지`가 안내 화면인 것은 **미구현이 아니라 PRD 5.7이
지시한 결과다.** 실제 API 호출, 가짜 통계, 임시 티어 결과를 넣지 않는다.

---

## 4. 관리자 챔피언 분류 ← 범위에 다시 들어옴

### 4.1 결정

v0.7에서 걷어냈던 **관리자 챔피언 분류를 범위에 되돌린다** (2026-09-02 결정).
PRD v0.8에 5.6절과 FR-34~43으로 복원했다. 고아로 남아 있던 용어 6개
(`챔피언 운영 상태`, `분류 검토 상태`, `데이터 소스 상태`, `분류 노출 상태`,
`분류 보관`, `미분류`)와 목표 6 "배포 없이 갱신", 대상 사용자의 "운영 사용자"가
다시 살아 있는 요구사항이 되었다.

미뤄 두었던 대안(목표에서 "배포 없이"를 빼는 안)은 채택하지 않았다.

### 4.2 이 결정이 코드에 요구하는 것

**분류가 코드에서 데이터베이스로 옮겨간다.** 이것이 이 결정의 실질이며, 지금 구조에서
가장 큰 변경이다.

| 지금 | 바뀐 뒤 |
| --- | --- |
| [`src/data/taxonomy.ts`](../src/data/taxonomy.ts)가 분류의 단일 원본 | D1이 단일 원본. `taxonomy.ts`는 초기 이관 자료로만 남는다 |
| 명단을 고치려면 파일 수정 + 배포 | 관리자 화면에서 즉시 반영 |
| `champions.ts`가 **모듈 로드 시점에** 이름을 카탈로그와 대조하고 틀리면 throw | 쓰기 시점 검증으로 옮긴다. 카탈로그에 없는 챔피언은 배정 자체가 막힌다 (FR-42) |
| 챔피언이 카탈로그에서 사라지면 앱이 뜨지 않는다 | `소스 누락 검토 필요`로 표시만 하고 서비스는 계속 뜬다 (FR-41) |

### 4.3 알려진 위험 — 220ms 전환

[`src/data/selection.ts`](../src/data/selection.ts)는 분류 전체(약 23KB, 배정 185건)를
직렬화해 첫 응답에 실어 보낸다. 포지션·카테고리 전환마다 서버를 왕복하면
`DESIGN_BLUEPRINT.md`가 규정한 **220ms 전환을 지킬 수 없기 때문이다.**

분류를 D1로 옮기면 이 데이터를 매 요청 조회해야 한다. 그대로 하면 첫 응답이 느려진다.

**이전 전후로 전환 시간을 실측해 비교할 것.** 느려지면 분류 조회 결과를 캐시한다.
분류는 관리자가 가끔 바꾸는 데이터라 캐시 적중률이 높고, 무효화 시점도 명확하다
(관리자가 저장할 때).

### 4.4 남은 작업

| 작업 | 관련 요구사항 |
| --- | --- |
| 동기화 검토 상태 — 새로 들어온 챔피언을 `검토 대기`로 두고, 카탈로그에서 사라진 챔피언을 `소스 누락 검토 필요`로 표시한다 | FR-40·41. **자동으로 내리지 않는다** — 동기화 사고와 실제 삭제를 기계가 구분할 수 없다 |
| 분류 변경 이력 화면 | FR-43 (P1). `champion_placements`와 `champion_ops`에 `updated_at`·`updated_by`가 이미 있고, `champion_ops.note`가 사유까지 갖고 있다. 두 표를 시간순으로 합쳐 보여 주는 화면 하나면 된다 |

들어간 것: 배정·이동·해제(FR-34·35·36·42, 마이그레이션 0004), 카테고리 안 표시 순서
(FR-38, `↑↓` 버튼), 챔피언 운영 상태(FR-39, 마이그레이션 0007). **FR-37(분류 보관)은
폐기했다** — 문서가 챔피언에 딸리면서 배정을 지워도 잃는 것이 없어졌다.

`selection.ts`가 분류 전체를 첫 응답에 싣는 구조는 그대로다. 전환 시간 실측(4.3)은
아직 하지 않았다.

관리자 역할 판정 수단(로그인 3단계 + `authGuard.ts`)은 이미 갖춰졌다. 위키 편집(4단계)과는
독립이므로 순서는 아무 때나 놓을 수 있다.

### 4.5 아직 정해지지 않은 것

**어떤 기준으로 배정할지는 여전히 미정이다** (PRD 15 미결정 1번). 관리자 화면은 갱신
*수단*을 제공할 뿐, 아리를 `mage`에 둘지 판단해 주지 않는다. 현재 분류는
`taxonomy.ts`에 담긴 초기 운영 분류이며 그 근거는 `archive/plan1.txt`의 미드 세 카테고리뿐이다.

---

## 5. 운영하면서 알아야 할 것

### 5.1 `dev`와 `build`를 동시에 돌리지 말 것

둘 다 같은 `.next` 디렉터리를 쓴다. dev 서버를 켠 채 `npm run build`를 돌리면 dev 서버가
빈 페이지나 500을 뱉기 시작한다. 이 경우 **코드 문제가 아니다.** dev 서버를 끄고
`.next`를 지운 뒤 다시 켜면 된다.

```bash
rm -rf .next && npm run dev
```

### 5.2 패치 갱신

**롤 패치가 나올 때마다 돌린다.** 패치는 2주 간격(2026년 24회)이고 수요일이나 목요일에
나온다. 미국 휴일(6월 말~7월 초)과 연말 휴식기에만 3주가 벌어진다. 책임자는 운영자다
(PRD 8장 "데이터 갱신").

```bash
DDRAGON_PATCH=16.18.1 npm run data:sync   # 챔피언·스킬 다시 내려받기
npx tsx scripts/check-taxonomy.ts          # 이름이 깨지지 않았는지 점검
npm run build                              # 생성 파일이 빌드를 깨지 않는지
npm run cf:deploy                          # 갱신은 배포해야 반영된다
```

**코드 배포가 있어야 반영된다.** 챔피언·스킬 데이터는 D1이 아니라 저장소의 생성
파일(`src/data/generated/`)에 있으므로, 내려받기만 하고 배포하지 않으면 운영에는
옛 수치가 그대로 남는다.

패치 버전과 갱신 시각은 `src/data/generated/champions.json`에 저장되고 사이트 하단에
표시된다. 갱신이 늦어도 사이트는 멈추지 않는다 — 읽는 사람이 지금 보는 수치가 언제
것인지 하단에서 확인할 수 있다.

신규 챔피언이 나오면 `taxonomy.ts`에 직접 배정해야 목록에 나온다.

### 5.3 챔피언 분류 갱신

> **이 절은 관리자 화면이 생기면 바뀐다.** 분류는 D1로 옮겨가고 `taxonomy.ts`는 초기
> 이관 자료로만 남는다 (4장). 아래는 그때까지의 방식이다.

포지션·카테고리와 챔피언 명단은 [`src/data/taxonomy.ts`](../src/data/taxonomy.ts)
**한 곳에서만** 관리한다. 화면 코드에 챔피언을 개별로 고정하지 않는다 (PRD 5.1).

- 이름은 Data Dragon `ko_KR` 표기와 **정확히** 일치해야 한다. `리 신`, `트위스티드 페이트`,
  `아우렐리온 솔`, `문도 박사`처럼 띄어쓰기가 있는 이름을 조심할 것.
- 틀리면 앱이 뜰 때 오류로 잡히고, `check-taxonomy.ts`가 어떤 이름인지 알려 준다.
- 한 챔피언이 여러 포지션에 속할 수 있지만 **같은 포지션 안에서는 카테고리 하나뿐**이다.
  점검 스크립트가 중복도 잡는다.
- 분류에 없는 조합(`/matchup/mid/leesin` 같은)은 404가 된다. 의도된 동작이다 (FR-07).

### 5.4 외부 의존성

| 대상 | 출처 | 확인해야 할 것 |
| --- | --- | --- |
| 챔피언 아이콘 · 일러스트 · 스킬 아이콘 | Riot Data Dragon CDN | 런타임에 외부 요청. 오프라인·차단 환경에서는 대체 UI가 뜬다 |
| Pretendard (본문) | jsDelivr CDN | 서체 라이선스 확인 (블루프린트 10장) |
| IBM Plex Mono (수치) | Google Fonts (`next/font/google`) | OFL |
| **SB 어그로체 B (제목)** | `src/app/fonts/SBAggroB.woff` self-host | **파일을 수정·서브셋하지 말 것.** 라이선스가 폰트 파일의 수정·복제·배포를 금지한다. 235KB를 통째로 전송하므로 LCP 예산에 포함해 볼 것 |
| 카테고리 대표 이미지 13장 | `public/images/` (챔피언 일러스트) | 확인 끝. Riot "Legal Jibber Jabber" 팬 프로젝트 정책 범위다 — 로고·상표를 쓰지 않고 지정 고지를 하단에 두는 조건이며 둘 다 지키고 있다 (PRD 15 해소 표) |

Riot 비공식 프로젝트 고지는 사이트 하단에 이미 넣어 두었다 (PRD 16).
공개 배포 전에 Riot 정책을 다시 검토할 것.

### 5.5 모바일 헤더 메뉴가 가로로 넘친다

360px에서 메뉴 다섯 개(`상대법 / 전적 / 통계 / 티어표 / 강의`)의 폭이 254px인데
로고와 마이페이지 아바타를 빼면 172px만 남는다. 세 개 반만 보이고 나머지는 가로 스크롤이다.
오른쪽 끝을 흐리게 처리해 더 있다는 것은 알 수 있게 했다.

글자와 여백을 최대로 줄여도 82px이 모자라므로, 제대로 풀려면 둘 중 하나가 필요하다.

1. 모바일에서 햄버거 메뉴로 전환한다 (블루프린트에 모바일 헤더 규격이 없어 새로 정해야 함)
2. 모바일 헤더에서는 `강의`처럼 우선순위가 낮은 항목을 빼고 마이페이지 안으로 옮긴다

지금은 스크롤로 모든 메뉴에 닿을 수 있어 기능상 막힌 곳은 없다.

**2026-09-03 로그인 UI 추가로 폭이 더 줄었다.** `마이페이지` 오른쪽에 로그인 상태
표시(`AuthStatus`)가 붙어 `nav`가 쓸 수 있는 폭이 조금 더 줄었다. 텍스트 하나(`로그인`)
또는 이름(말줄임)+`로그아웃`만 두어 최소화했지만, 위 문제를 새로 만들지는 않되 악화는
시킨다. 햄버거 메뉴 전환(위 1번)을 하게 되면 이 요소도 같이 옮길 것.

### 5.6 브라우저 저장소 키 — 더 이상 쓰지 않는다

옛 Tip 게시판이 쓰던 키다. 위키 전환 2단계에서 `tipStore.ts`와 함께 걷어냈으므로 앱은
이제 아무것도 읽고 쓰지 않는다. **이전에 사이트를 방문한 브라우저에는 값이 남아 있다.**

```
kkaenam.tips.v1     내가 쓴 Tip
kkaenam.votes.v1    좋아요 · 싫어요
kkaenam.user.v1     로컬 데모 계정
```

남은 값은 동작에 영향을 주지 않지만, 정리하려면 개발자 도구에서 삭제하면 된다.
3단계에서 로그인을 붙일 때 세션 키가 새로 생기며 이 키들과는 무관하다.

### 5.7 D1 데이터베이스

상대법 저장소는 Cloudflare D1 `kkaenam-gg` (APAC)이다.

```bash
npx wrangler d1 migrations apply kkaenam-gg --local    # 로컬 스키마
npx wrangler d1 migrations apply kkaenam-gg --remote   # 운영 스키마
npx wrangler d1 execute kkaenam-gg --remote --command "SELECT COUNT(*) FROM wiki_docs;"
```

**새 마이그레이션 번호는 로컬 파일이 아니라 `origin/main`을 보고 정한다.** 로컬이 뒤처진
채로 다음 번호를 붙이면 같은 번호가 두 벌 생긴다. 실제로 2026-09-06에 알림 마이그레이션을
0008로 만들었다가 `origin/main`에 이미 `0008_wiki_links.sql`이 있는 것을 보고 0009로
옮겼다. wrangler는 번호가 아니라 **파일 이름**으로 적용 여부를 판정해서 둘 다 적용되지만,
그때부터 적용 순서와 파일 순서가 어긋난다.

`0009_notification_read_marker.sql`(알림 읽음 표시)은 로컬·운영 모두 적용했다
(2026-09-06). 헤더의 종을 그리는 질의가 이 열을 읽고 **헤더는 사이트 전체에 얹혀
있으므로**, 이 계열의 마이그레이션은 코드를 배포하기 전에 원격에 먼저 넣어야 한다.
순서가 뒤바뀌면 로그인한 사람에게 모든 화면이 깨진다.

**운영 데이터를 손으로 고친 기록.** 2026-09-06, 분류 문서 세 개의 `분류:` 접두사를
뗐다 (`분류:라인전` → `라인전` 등). 위계가 문서에서 나오게 된 뒤로 접두사는 아무 일도
하지 않는데(부모 키는 접두사를 뗐다 다시 붙여 짓는다), 화면과 주소에만 남아 있었다.
`wiki_links`는 건드리지 않았다 — 본문의 `[[분류:라인전]]` 태그는 문서 이름과 무관하게
같은 키에 닿는다. 스키마 변경이 아니므로 마이그레이션으로 만들지 않았고, 되돌릴 때
쓸 원래 값은 이 문단이 기록이다: `doc-a-35df…` = 라인전, `doc-a-52dc…` = 라인관리,
`doc-a-b1df…` = 딜교환.

**생성된 시드 SQL을 `migrations/`에 두지 말 것.** wrangler가 그 디렉터리의 `.sql`을 전부
마이그레이션으로 취급해 나중에 `migrations apply`를 돌릴 때 시드가 다시 실행된다.
`seeds/`에 두는 이유가 이것이다.

---

## 6. 눈으로 확인이 남은 것

데스크톱은 2026-09-01에 브라우저로 확인했다. 그 과정에서 포스터 가로 확장이 CSS 특이도
문제로 죽어 있던 것, 스킬 쪽지에 템플릿 자리표시자가 노출되던 것, 헤드라인이 포지션 탭을
덮던 것을 찾아 고쳤다. **빌드·타입 검사만으로는 셋 다 드러나지 않았다.**

아래는 아직 눈으로 보지 못했거나 다시 봐야 하는 항목이다.

- [x] 디스플레이 서체와 Pretendard의 한글 조합 (2026-09-01 확인, SB 어그로체 B)
- [ ] 긴 카테고리명 `브루저/원딜`의 포스터 라벨이 좁아진 포스터에서 깨지지 않는지
- [ ] 다섯 포지션 12개 카테고리에서 비대칭 포스터 조합이 무너지지 않는지
- [ ] 포스터 가로 확장·복원, 축소된 포스터의 라벨이 읽히는지, 키보드 포커스에서도 동작하는지
- [ ] 챔피언 일러스트의 데스크톱·모바일 크롭. 얼굴이 잘리는 챔피언은
      `champions.ts`의 `FOCUS_OVERRIDES`에 추가한다
- [x] **360px 모바일** (2026-09-02 확인). 포스터 세로 쌓기, 챔피언 4열 그리드, Aside → Main
      순서, 게시판·글쓰기 모두 정상. 확인 중 헤더 버그 두 건을 찾아 고쳤다
- [ ] 모바일 헤더 메뉴가 가로로 넘친다 — 아래 5.6 참조
- [ ] 색상 대비와 포커스 표시
- [ ] `prefers-reduced-motion`을 켠 상태

화면 캡처는 아래 명령으로 다시 뽑을 수 있다. 결과는 `.screenshots/`에 저장되고 git에는 올리지 않는다.

```bash
npm run dev            # 다른 터미널에서 먼저
npm run shots          # 모바일 360 / 태블릿 820 / 데스크톱 1440
npm run shots -- mobile
```

확인용 주소:

```
http://localhost:3000/                          포지션·카테고리 선택
http://localhost:3000/?position=support         포스터 3장 (Mom / Dad / Wtf?!)
http://localhost:3000/?position=adc             포스터 1장 + 설명 영역
http://localhost:3000/matchup/mid/ahri          상대법 화면
http://localhost:3000/matchup/mid/ahri?me=twistedfate   Me 노출 확인
```

---

## 7. 출시 전 확인 (PRD 12 기준)

| PRD 12 기준 | 상태 |
| --- | --- |
| P0 기능 구현과 주요 흐름 테스트 | 탐색·열람·로그인·편집·검토 구현 완료. **실제 브라우저 클릭 검증 미완료** — 2.1 참고 |
| 다섯 포지션 메뉴, 기본 미드, URL 복원 | 완료 |
| 12개 카테고리 커버 이미지 매핑 | 완료 (점검 스크립트가 검증) |
| 포지션 → 카테고리 → 챔피언 → 문서 → 편집 흐름 | 코드 구현 완료. 브라우저 클릭 검증 미완료 |
| 아리 페이지 일러스트 · 챔피언명 · Q/W/E/R | 완료 |
| 상대법 문서 기본 표시, 영상 탭 상태 복원 | 완료 |
| Me 미선택 시 공통만, 트페 선택 시 전용 내용만 | 완료 (검증함) |
| 구글·카카오 로그인 | **완료** (2026-09-03) — 2.1 참고. 실제 OAuth 왕복은 로컬에서 수동 확인 필요 |
| 지운 것 없는 편집 즉시 반영 / 지운 편집 검토 대기 | **구현 완료** (2026-09-04) — `wikiDiff`의 판정을 단위 수준으로 검증함. 로컬 D1 왕복은 재검증 필요 |
| 편집 화면 좌우 형광펜 (FR-48) | **구현 완료** (2026-09-04) — 브라우저에서 한글 조합 입력 중 형광펜 정렬을 눈으로 확인할 것 |
| 섹션 비우기가 검토 대기로 가는지 (우회로 없음) | **구현 완료** — 검증함 |
| 문서 역사 · 되돌리기 · 최근 변경 · 제출 제한 | **구현 완료** — 되돌리기의 섹션별 재구성과 제출 제한 카운트 쿼리를 D1에서 직접 검증함 |
| 관리자 분류 화면, 재배포 없는 반영 | **미구현** — 4장 |
| 비관리자의 분류 관리 접근 차단 | **미구현** — 4장 (단, 재사용 가능한 `authGuard.ts`는 이미 있음) |
| 모바일·데스크톱 브라우저 UI 오류 없음 | 데스크톱 확인 완료 (2026-09-01/02). **편집 UI 포함 모바일 재확인 필요** — 6장 |
| 로그인하지 않은 편집 제출과 비운영자 검토 차단 | **구현 완료** — 서버 사이드 게이팅을 실제 세션으로 검증함(리다이렉트/404) |
| 악성 입력 차단 | **구현 완료** — 마크다운 렌더러가 `<script>`/`onerror`/`javascript:`/`data:` 벡터를 무해하게 처리함을 확인 |
| 로딩 · 빈 상태 · 오류 상태 | 완료 |
| 전적·통계·티어표 안내 화면, 가짜 데이터 없음 | 완료 |
| 이미지 사용 권한 · 운영 정책 · 개인정보 고지 | 개인정보처리방침·이용약관·문의 페이지 완료 (2026-09-03). 이미지 사용 권한은 **미확인** — 5.4 |

출시를 막고 있는 것은 **편집 기능의 실제 브라우저 클릭 검증(2.1)**, **관리자 분류(4장)**,
**에셋 사용 권리(5.4)**, **시각 검수(6장, 편집 UI 포함)** 네 가지다.
서버·데이터베이스·로그인·편집·검토 로직은 해결됐다.
